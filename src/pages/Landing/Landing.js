import React from 'react';
import Layout from '../../components/Layout/Layout';

const Landing = () => {
    return (
    <Layout noSideBar>
        <div>
            Landing page
        </div>
    </Layout>
    );
}
export default Landing;
